# OSINT Toolkit - Зависимости
# Установка ядра:  pip install -r requirements.txt
#
# Здесь только то, что РЕАЛЬНО импортируется кодом. Все пакеты ставятся
# готовыми wheel-ами и НЕ требуют компилятора C++ — установка проходит
# на Windows/Linux/macOS без "Microsoft Visual C++ Build Tools".

# --- Ядро (обязательно) ---------------------------------------------------
requests>=2.28.0          # HTTP-запросы (osint, username_checker, webapp)
python-whois>=0.7.3       # WHOIS (модуль whois)
dnspython>=2.2.1          # DNS-запросы (модуль dns)
beautifulsoup4>=4.11.1    # Парсинг HTML (модуль bs4)
Flask>=3.0.0              # Веб-версия (webapp.py)


# --- Опционально (раскомментируйте при необходимости) ---------------------
# Ускоренный парсер для BeautifulSoup:
# lxml>=4.9.1
#
# Корректные ANSI-цвета в старых терминалах Windows:
# colorama>=0.4.5
#
# Прогресс-бары / таблицы (если будете расширять CLI):
# tqdm>=4.64.0
# tabulate>=0.9.0
# psutil>=5.9.0
#
# Криптография (для будущих модулей; pycryptodome ставится wheel-ом):
# cryptography>=37.0.0
# pycryptodome>=3.15.0


# --- Сетевые инструменты (ТРЕБУЮТ компилятор / админ-права) ----------------
# Нужны только для расширенного сетевого сканирования. На Windows требуют
# "Microsoft C++ Build Tools" (netifaces) и Npcap (scapy). Сейчас кодом
# напрямую НЕ используются — ставьте вручную, только если они вам нужны:
# netifaces>=0.11.0
# python-nmap>=0.7.1        # также требует установленный Nmap в системе
# scapy>=2.4.5
